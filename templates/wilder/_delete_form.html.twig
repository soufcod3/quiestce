<form method="post" action="{{ path('wilder_delete', {'id': wilder.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wilder.id) }}">
    <button class="btn">Delete</button>
</form>
